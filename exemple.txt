// Example program with deliberate lexical and syntactic errors

PROGRAM Test // Lexical Error: Identifier exceeds 7 characters
VAR {
    INTEGER x, y, z1; // Lexical Error: Identifier exceeds 7 characters
    FLOAT var;           // Lexical Error: Invalid identifier starting with a digit
    CONST pi = 2;    // Syntax Error: Invalid constant format
    INTEGER a, b;      
}
BEGIN
{
 x=5.5;              // Syntax Error: Assigning FLOAT to INTEGER
y=( x + 2 );            // Syntax Error: Missing closing parenthesis
    z = a + b;            // Syntax Error: Use of undeclared variables
    a = 10 / (5 - 5);     // Runtime Error: Division by zero (handled during runtime if implemented)
    
    IF (x > 0) {          // Syntax Error: Missing closing bracket
        b = b + 1;
    }
ELSE {                // Syntax Error: Missing IF block
        a = a - 1;
    }

    FOR (i : 0: 1 : 5)
    {       // Syntax Error: Missing condition or closing parenthesis
    b = b + 1;}

    WHILE (z != 0 ){       // Syntax Error: Missing closing parenthesis
        z = z - 1;
    }

    WRITELN('Value of x: ' x); 
    }// Syntax Error: Invalid function name (writeln expected)
END                        // Syntax Error: Missing period